console.log(
  "This project uses Vercel API routes. Check /api/send-email.js for the email functionality."
);

// Export a simple function (won't be used but satisfies Node.js module requirements)
module.exports = {
  message: "This project uses Vercel API routes",
};
